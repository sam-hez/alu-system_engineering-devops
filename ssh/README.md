# SSH

This project introduces SSH, servers, RSA key pairs, and public-key
authentication.

## Task 0

`0-use_a_private_key` connects to the assigned Ubuntu server using SSH,
the `ubuntu` account, and the private key stored at `~/.ssh/school`.
