# Web Stack Debugging 0

This project introduces systematic web-stack debugging with Docker and
Apache.

The supplied container has Apache installed and a configured homepage,
but the web server is not initially running. `0-give_me_a_page` starts
Apache so that port 80 returns `Hello Holberton`.